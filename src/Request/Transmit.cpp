// Transmit.cpp
// Brief introduction: the implementation of Transmit's method
// Create by Zhang Zhecheng 2020/5/18
#include "Request.h"
namespace request
{
    /*
    Transmit::Transmit(const string &forwarder,const Request &request)
        : Request(Kind::Transmit), _forwarder(forwarder), _request(request.clone()){}
    bool Transmit::hasAttribute(Attribute atr) const
    {
        bool ret;
        switch (atr)
        {
        case Attribute::Forwarder:
        case Attribute::Request:
            ret = true;
            break;
        default : ret=Request::hasAttribute(atr);break;
        }
        return ret;
    }
    */
} // namespace request
